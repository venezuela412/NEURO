# AGENTS

## Cursor Cloud specific instructions

This is a newly initialized repository ("NEURO") with no application code yet. The repo contains only a `README.md`.

- **No services to start.** There are no backend, frontend, or database services.
- **No dependencies to install.** There are no `package.json`, `requirements.txt`, `pyproject.toml`, or other dependency manifests.
- **No lint, test, or build commands** are configured.

When code is added, update this section with:
1. How to install dependencies and which package manager/toolchain to use.
2. How to run lint, tests, and builds.
3. How to start development servers and any required auxiliary services.
4. Any non-obvious caveats discovered during setup.
