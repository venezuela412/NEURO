#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; tact_NeuroMaster.headers.fc
;;
;; Header files for NeuroMaster
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_store_address_opt
builder __tact_store_address_opt(builder b, slice address) inline;

;; __tact_not_null
forall X -> X __tact_not_null(X x) impure inline;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; $DeployOk$_store
builder $DeployOk$_store(builder build_0, (int) v) inline;

;; $DeployOk$_store_cell
cell $DeployOk$_store_cell((int) v, builder b) inline;

;; $TokenTransferInternal$_store
builder $TokenTransferInternal$_store(builder build_0, (int, int, slice, slice, int, slice) v) inline;

;; $TokenTransferInternal$_store_cell
cell $TokenTransferInternal$_store_cell((int, int, slice, slice, int, slice) v, builder b) inline;

;; $TokenExcesses$_store
builder $TokenExcesses$_store(builder build_0, (int) v) inline;

;; $TokenExcesses$_store_cell
cell $TokenExcesses$_store_cell((int) v, builder b) inline;

;; $NeuroMaster$_store
builder $NeuroMaster$_store(builder build_0, (int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) v) inline;

;; $NeuroMaster$_load
(slice, ((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int))) $NeuroMaster$_load(slice sc_0) inline;

;; $StateInit$_get_code
_ $StateInit$_get_code((cell, cell) v) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $JettonData$_to_external
(int, int, slice, cell, cell) $JettonData$_to_external(((int, int, slice, cell, cell)) v) inline;

;; $NeuroWallet$init$_store
builder $NeuroWallet$init$_store(builder build_0, (slice, slice) v) inline;

;; $NeuroMaster$init$_load
(slice, ((slice, slice, cell))) $NeuroMaster$init$_load(slice sc_0) inline;

;; $NeuroMaster$_contract_init
(int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $NeuroMaster$_contract_init(slice $owner, slice $keeper, cell $jetton_content) impure inline;

;; $NeuroMaster$_contract_load
(int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $NeuroMaster$_contract_load() impure inline;

;; $NeuroMaster$_contract_store
() $NeuroMaster$_contract_store((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) v) impure inline;

;; $Cell$_fun_asSlice
slice $Cell$_fun_asSlice(cell $self) impure inline;

;; $global_newAddress
slice $global_newAddress(int $chain, int $hash) impure inline;

;; $global_contractAddressExt
slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline;

;; $global_contractAddress
slice $global_contractAddress((cell, cell) $s) impure inline;

;; $NeuroWallet$_init_child
(cell, cell) $NeuroWallet$_init_child(slice $master, slice $owner) inline_ref;

;; $MessageParameters$_constructor_bounce_to_value_mode_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline;

;; $NeuroMaster$_fun_isWhitelisted
((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), int) $NeuroMaster$_fun_isWhitelisted((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self, slice $addr) impure inline_ref;

;; $JettonData$_constructor_totalSupply_mintable_owner_content_walletCode
((int, int, slice, cell, cell)) $JettonData$_constructor_totalSupply_mintable_owner_content_walletCode(int $totalSupply, int $mintable, slice $owner, cell $content, cell $walletCode) inline;

;; $NeuroMaster$_fun_get_jetton_data
((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), (int, int, slice, cell, cell)) $NeuroMaster$_fun_get_jetton_data((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self) impure inline_ref;

;; $NeuroMaster$_fun_get_wallet_address
((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), slice) $NeuroMaster$_fun_get_wallet_address((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self, slice $owner) impure inline_ref;

;; $NeuroMaster$_fun_tvl
((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), int) $NeuroMaster$_fun_tvl((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self) impure inline_ref;

;; $NeuroMaster$_fun_sharePrice
((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), int) $NeuroMaster$_fun_sharePrice((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self) impure inline_ref;

;; $NeuroMaster$_fun_isPaused
((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), int) $NeuroMaster$_fun_isPaused((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self) impure inline_ref;

;; $NeuroMaster$_fun_totalAssets
((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), int) $NeuroMaster$_fun_totalAssets((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self) impure inline_ref;

;; $NeuroMaster$_fun_notify
((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), ()) $NeuroMaster$_fun_notify((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self, cell $body) impure inline;

;; $SendParameters$_constructor_to_value_mode_bounce_body_code_data
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce_body_code_data(slice $to, int $value, int $mode, int $bounce, cell $body, cell $code, cell $data) inline;

;; $TokenTransferInternal$_constructor_queryId_amount_from_response_destination_forward_ton_amount_forward_payload
((int, int, slice, slice, int, slice)) $TokenTransferInternal$_constructor_queryId_amount_from_response_destination_forward_ton_amount_forward_payload(int $queryId, int $amount, slice $from, slice $response_destination, int $forward_ton_amount, slice $forward_payload) inline;

;; $SendParameters$_constructor_to_value_mode_bounce_body
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce_body(slice $to, int $value, int $mode, int $bounce, cell $body) inline;

;; $TokenExcesses$_constructor_queryId
((int)) $TokenExcesses$_constructor_queryId(int $queryId) inline;

;; $DeployOk$_constructor_queryId
((int)) $DeployOk$_constructor_queryId(int $queryId) inline;


;; tact_NeuroMaster.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(slice, slice) __tact_load_address_opt(slice cs) asm """

            b{00} SDBEGINSQ
            IF:<{
              PUSHNULL
            }>ELSE<{
              LDMSGADDR
              SWAP
            }>

""";

builder __tact_store_addr_none(builder b) asm "b{00} STSLICECONST";

builder __tact_store_address_opt(builder b, slice address) inline {
    if (null?(address)) {
        return __tact_store_addr_none(b);
    } else {
        return b.store_slice(address);
    }
}

forall X -> X __tact_not_null(X x) impure inline {
    throw_if(128, null?(x)); return x;
}

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

builder $global_beginCell() impure asm """
    NEWC
""";

slice $global_emptySlice() impure asm """
    b{} PUSHSLICE
""";

int $global_contractHash(cell $code, cell $data) impure asm """
    s0 PUSH HASHCU // `data` hash
    s2 PUSH HASHCU // `code` hash
    SWAP2
    CDEPTH         // `data` depth
    SWAP
    CDEPTH         // `code` depth
    131380 INT     // (2 << 16) | (1 << 8) | 0x34

    // Group 2: Composition of the Builder
    NEWC
    24 STU  // store refs_descriptor | bits_descriptor | data
    16 STU  // store depth_descriptor for `code`
    16 STU  // store depth_descriptor for `data`
    256 STU // store `code` hash
    256 STU // store `data` hash

    // Group 3: SHA256 hash of the resulting Builder
    ONE HASHEXT_SHA256
""";

slice $Slice$_fun_asAddressUnsafe(slice $self) impure asm "NOP";

slice $Cell$_fun_beginParse(cell $self) impure asm """
    CTOS
""";

slice $Cell$_fun_asSlice(cell $self) impure inline {
    var ($self) = $self;
    return $Cell$_fun_beginParse($self);
}

cell $Builder$_fun_endCell(builder $self) impure asm """
    ENDC
""";

slice $global_newAddress(int $chain, int $hash) impure inline {
    return $Slice$_fun_asAddressUnsafe($Cell$_fun_asSlice($Builder$_fun_endCell(store_uint(store_int(store_uint($global_beginCell(), 4, 3), $chain, 8), $hash, 256))));
}

slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline {
    int $hash = $global_contractHash($code, $data);
    return $global_newAddress($chain, $hash);
}

slice $global_contractAddress((cell, cell) $s) impure inline {
    var (($s'code, $s'data)) = $s;
    return $global_contractAddressExt(0, $s'code, $s'data);
}

slice $global_myAddress() impure asm """
    MYADDR
""";

int $global_myBalance() impure asm """
    BALANCE FIRST
""";

() $global_nativeReserve(int $amount, int $mode) impure asm """
    RAWRESERVE
""";

() $global_message((int, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    106 PUSHINT         // 1 + 4 + 4 + 64 + 32 + 1
    STZEROES
    // → Stack state
    // s0: Builder
    // s1: `body`
    // s2: `mode`
    STDICT
    ENDC
    SWAP
    SENDRAWMSG
""";

() $global_send((int, cell, cell, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    105 PUSHINT         // 1 + 4 + 4 + 64 + 32
    STZEROES            // store currency_collection, ihr_fee, fwd_fee, created_lt and created_at
    // → Stack state
    // s0: Builder
    // s1: `data`
    // s2: `code`
    // s3: `body`
    // s4: `mode`

    // Group 2: Placing the Builder after code and data, then checking those for nullability
    s2 XCHG0
    DUP2
    ISNULL
    SWAP
    ISNULL
    AND
    // → Stack state
    // s0: -1 (true) if `data` and `code` are both null, 0 (false) otherwise
    // s1: `code`
    // s2: `data`
    // s3: Builder
    // s4: `body`
    // s5: `mode`

    // Group 3: Left branch of the IFELSE, executed if s0 is -1 (true)
    <{
        DROP2 // drop `data` and `code`, since either of those is null
        b{0} STSLICECONST
    }> PUSHCONT

    // Group 3: Right branch of the IFELSE, executed if s0 is 0 (false)
    <{
        // _ split_depth:(Maybe (## 5))
        //   special:(Maybe TickTock)
        //   code:(Maybe ^Cell)
        //   data:(Maybe ^Cell)
        //   library:(Maybe ^Cell)
        // = StateInit;
        ROT                // place message Builder on top
        b{10} STSLICECONST // store Maybe = true, Either = false
        // Start composing inlined StateInit
        b{00} STSLICECONST // store split_depth and special first
        STDICT             // store code
        STDICT             // store data
        b{0} STSLICECONST  // store library
    }> PUSHCONT

    // Group 3: IFELSE that does the branching shown above
    IFELSE
    // → Stack state
    // s0: Builder
    // s1: null or StateInit
    // s2: `body`
    // s3: `mode`

    // Group 4: Finalizing the message
    STDICT // store `body` as ref with an extra Maybe bit, since `body` might be null
    ENDC
    // → Stack state
    // s0: Cell
    // s1: `mode`

    // Group 5: Sending the message, with `mode` on top
    SWAP
    SENDRAWMSG // https://github.com/tact-lang/tact/issues/1558
""";

((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), ()) $NeuroMaster$_fun_notify((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self, cell $body) impure inline {
    var (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent)) = $self;
    slice $to = __tact_context_get_sender();
    int $bounce = false;
    if (false) {
        int $balance = $global_myBalance();
        int $balanceBeforeMessage = ($balance - $Context$_get_value(__tact_context_get()));
        if (($balanceBeforeMessage < 0)) {
            $global_nativeReserve(0, 0);
            $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 130, $body));
            return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), ());
        }
    }
    $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 66, $body));
    return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), ());
}

;; tact_NeuroMaster.constants.fc
;; Cell PUbhb3vBPozKDI3CDKNVXoOAoEff7W8S0ZG9UiDTpMM=
cell __gen_cell_cell_3d46e16f7bc13e8cca0c8dc20ca3555e8380a047dfed6f12d191bd5220d3a4c3() asm """
    B{b5ee9c7241010101001000001c000000005769746864726177616c3d0841cb} B>boc PUSHREF
""";

;; Cell Wmhpjey6LAsawM3wBPs4pZSZcGidO+18sefjHLyJIUk=
cell __gen_cell_cell_5a68698decba2c0b1ac0cdf004fb38a5949970689d3bed7cb1e7e31cbc892149() asm """
    B{b5ee9c7241010101000f00001a00000000526562616c616e6365e733ba51} B>boc PUSHREF
""";

;; tact_NeuroMaster.storage.fc
;;
;; Type: StateInit
;; TLB: _ code:^cell data:^cell = StateInit
;;

_ $StateInit$_get_code((cell, cell) v) inline {
    var (v'code, v'data) = v;
    return v'code;
}

;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: SendParameters
;; TLB: _ mode:int257 body:Maybe ^cell code:Maybe ^cell data:Maybe ^cell value:int257 to:address bounce:bool = SendParameters
;;

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce_body_code_data(slice $to, int $value, int $mode, int $bounce, cell $body, cell $code, cell $data) inline {
    return ($mode, $body, $code, $data, $value, $to, $bounce);
}

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce_body(slice $to, int $value, int $mode, int $bounce, cell $body) inline {
    return ($mode, $body, null(), null(), $value, $to, $bounce);
}

;;
;; Type: MessageParameters
;; TLB: _ mode:int257 body:Maybe ^cell value:int257 to:address bounce:bool = MessageParameters
;;

((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

;;
;; Type: DeployOk
;; Header: 0xaff90f57
;; TLB: deploy_ok#aff90f57 queryId:uint64 = DeployOk
;;

builder $DeployOk$_store(builder build_0, (int) v) inline {
    var (v'queryId) = v;
    build_0 = store_uint(build_0, 2952335191, 32);
    build_0 = build_0.store_uint(v'queryId, 64);
    return build_0;
}

cell $DeployOk$_store_cell((int) v, builder b) inline {
    return $DeployOk$_store(b, v).end_cell();
}

((int)) $DeployOk$_constructor_queryId(int $queryId) inline {
    return ($queryId);
}

;;
;; Type: TokenTransferInternal
;; Header: 0x178d4519
;; TLB: token_transfer_internal#178d4519 queryId:uint64 amount:coins from:address response_destination:address forward_ton_amount:coins forward_payload:remainder<slice> = TokenTransferInternal
;;

builder $TokenTransferInternal$_store(builder build_0, (int, int, slice, slice, int, slice) v) inline {
    var (v'queryId, v'amount, v'from, v'response_destination, v'forward_ton_amount, v'forward_payload) = v;
    build_0 = store_uint(build_0, 395134233, 32);
    build_0 = build_0.store_uint(v'queryId, 64);
    build_0 = build_0.store_varuint16(v'amount);
    build_0 = build_0.store_slice(v'from);
    build_0 = __tact_store_address_opt(build_0, v'response_destination);
    build_0 = build_0.store_varuint16(v'forward_ton_amount);
    build_0 = build_0.store_slice(v'forward_payload);
    return build_0;
}

cell $TokenTransferInternal$_store_cell((int, int, slice, slice, int, slice) v, builder b) inline {
    return $TokenTransferInternal$_store(b, v).end_cell();
}

((int, int, slice, slice, int, slice)) $TokenTransferInternal$_constructor_queryId_amount_from_response_destination_forward_ton_amount_forward_payload(int $queryId, int $amount, slice $from, slice $response_destination, int $forward_ton_amount, slice $forward_payload) inline {
    return ($queryId, $amount, $from, $response_destination, $forward_ton_amount, $forward_payload);
}

;;
;; Type: TokenExcesses
;; Header: 0xd53276db
;; TLB: token_excesses#d53276db queryId:uint64 = TokenExcesses
;;

builder $TokenExcesses$_store(builder build_0, (int) v) inline {
    var (v'queryId) = v;
    build_0 = store_uint(build_0, 3576854235, 32);
    build_0 = build_0.store_uint(v'queryId, 64);
    return build_0;
}

cell $TokenExcesses$_store_cell((int) v, builder b) inline {
    return $TokenExcesses$_store(b, v).end_cell();
}

((int)) $TokenExcesses$_constructor_queryId(int $queryId) inline {
    return ($queryId);
}

;;
;; Type: JettonData
;; TLB: _ totalSupply:int257 mintable:bool owner:address content:^cell walletCode:^cell = JettonData
;;

(int, int, slice, cell, cell) $JettonData$_to_external(((int, int, slice, cell, cell)) v) inline {
    var (v'totalSupply, v'mintable, v'owner, v'content, v'walletCode) = v; 
    return (v'totalSupply, v'mintable, v'owner, v'content, v'walletCode);
}

((int, int, slice, cell, cell)) $JettonData$_constructor_totalSupply_mintable_owner_content_walletCode(int $totalSupply, int $mintable, slice $owner, cell $content, cell $walletCode) inline {
    return ($totalSupply, $mintable, $owner, $content, $walletCode);
}

;;
;; Type: NeuroWallet
;; TLB: _ balance:coins owner:address master:address = NeuroWallet
;;

builder $NeuroWallet$init$_store(builder build_0, (slice, slice) v) inline {
    var (v'master, v'owner) = v;
    build_0 = build_0.store_slice(v'master);
    build_0 = build_0.store_slice(v'owner);
    return build_0;
}

cell $NeuroWallet$_child_get_code() impure asm """
    B{} B>boc PUSHREF
""";

(cell, cell) $NeuroWallet$_init_child(slice $master, slice $owner) inline_ref {
    ;; Build init code cell

    ;; Contract Code: NeuroWallet
    cell init_code = $NeuroWallet$_child_get_code();


    builder b = begin_cell();
    b = b.store_int(false, 1);
    b = $NeuroWallet$init$_store(b, ($master, $owner));
    return (init_code, b.end_cell());
}

;;
;; Type: NeuroMaster
;; TLB: _ total_supply:coins total_assets:coins owner:address keeper:address paused:bool jetton_content:^cell mintable:bool whitelist1:address whitelist2:address whitelist3:address whitelist4:address whitelist5:address maxDelegatePercent:uint8 = NeuroMaster
;;

builder $NeuroMaster$_store(builder build_0, (int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) v) inline {
    var (v'total_supply, v'total_assets, v'owner, v'keeper, v'paused, v'jetton_content, v'mintable, v'whitelist1, v'whitelist2, v'whitelist3, v'whitelist4, v'whitelist5, v'maxDelegatePercent) = v;
    build_0 = build_0.store_varuint16(v'total_supply);
    build_0 = build_0.store_varuint16(v'total_assets);
    build_0 = build_0.store_slice(v'owner);
    build_0 = build_0.store_slice(v'keeper);
    build_0 = build_0.store_int(v'paused, 1);
    build_0 = build_0.store_ref(v'jetton_content);
    build_0 = build_0.store_int(v'mintable, 1);
    var build_1 = begin_cell();
    build_1 = __tact_store_address_opt(build_1, v'whitelist1);
    build_1 = __tact_store_address_opt(build_1, v'whitelist2);
    build_1 = __tact_store_address_opt(build_1, v'whitelist3);
    var build_2 = begin_cell();
    build_2 = __tact_store_address_opt(build_2, v'whitelist4);
    build_2 = __tact_store_address_opt(build_2, v'whitelist5);
    build_2 = build_2.store_uint(v'maxDelegatePercent, 8);
    build_1 = store_builder_ref(build_1, build_2);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

(slice, ((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int))) $NeuroMaster$_load(slice sc_0) inline {
    var v'total_supply = sc_0~load_varuint16();
    var v'total_assets = sc_0~load_varuint16();
    var v'owner = sc_0~load_msg_addr();
    var v'keeper = sc_0~load_msg_addr();
    var v'paused = sc_0~load_int(1);
    var v'jetton_content = sc_0~load_ref();
    var v'mintable = sc_0~load_int(1);
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'whitelist1 = sc_1~__tact_load_address_opt();
    var v'whitelist2 = sc_1~__tact_load_address_opt();
    var v'whitelist3 = sc_1~__tact_load_address_opt();
    slice sc_2 = sc_1~load_ref().begin_parse();
    var v'whitelist4 = sc_2~__tact_load_address_opt();
    var v'whitelist5 = sc_2~__tact_load_address_opt();
    var v'maxDelegatePercent = sc_2~load_uint(8);
    return (sc_0, (v'total_supply, v'total_assets, v'owner, v'keeper, v'paused, v'jetton_content, v'mintable, v'whitelist1, v'whitelist2, v'whitelist3, v'whitelist4, v'whitelist5, v'maxDelegatePercent));
}

(slice, ((slice, slice, cell))) $NeuroMaster$init$_load(slice sc_0) inline {
    var v'owner = sc_0~load_msg_addr();
    var v'keeper = sc_0~load_msg_addr();
    var v'jetton_content = sc_0~load_ref();
    return (sc_0, (v'owner, v'keeper, v'jetton_content));
}

(int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $NeuroMaster$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$NeuroMaster$_load();
    }
    else {
        (slice $owner, slice $keeper, cell $jetton_content) = $sc~$NeuroMaster$init$_load();
        $sc.end_parse();
        return $NeuroMaster$_contract_init($owner, $keeper, $jetton_content);
    }
}

() $NeuroMaster$_contract_store((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $NeuroMaster$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract NeuroMaster functions
;;

(int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $NeuroMaster$_contract_init(slice $owner, slice $keeper, cell $jetton_content) impure inline {
    var (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent)) = (null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null());
    $self'total_supply = 0;
    $self'total_assets = 0;
    $self'owner = $owner;
    $self'keeper = $keeper;
    $self'jetton_content = $jetton_content;
    $self'mintable = true;
    $self'paused = false;
    $self'maxDelegatePercent = 50;
    $self'whitelist1 = null();
    $self'whitelist2 = null();
    $self'whitelist3 = null();
    $self'whitelist4 = null();
    $self'whitelist5 = null();
    return ($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent);
}

((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), int) $NeuroMaster$_fun_isWhitelisted((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self, slice $addr) impure inline_ref {
    var (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent)) = $self;
    if (( ((~ null?($self'whitelist1))) ? (( equal_slices_bits(__tact_not_null($self'whitelist1), $addr) )) : (false) )) {
        var $fresh$ret_15 = true;
        return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_15);
    }
    if (( ((~ null?($self'whitelist2))) ? (( equal_slices_bits(__tact_not_null($self'whitelist2), $addr) )) : (false) )) {
        var $fresh$ret_16 = true;
        return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_16);
    }
    if (( ((~ null?($self'whitelist3))) ? (( equal_slices_bits(__tact_not_null($self'whitelist3), $addr) )) : (false) )) {
        var $fresh$ret_17 = true;
        return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_17);
    }
    if (( ((~ null?($self'whitelist4))) ? (( equal_slices_bits(__tact_not_null($self'whitelist4), $addr) )) : (false) )) {
        var $fresh$ret_18 = true;
        return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_18);
    }
    if (( ((~ null?($self'whitelist5))) ? (( equal_slices_bits(__tact_not_null($self'whitelist5), $addr) )) : (false) )) {
        var $fresh$ret_19 = true;
        return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_19);
    }
    var $fresh$ret_20 = false;
    return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_20);
}

((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), (int, int, slice, cell, cell)) $NeuroMaster$_fun_get_jetton_data((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self) impure inline_ref {
    var (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent)) = $self;
    var $fresh$ret_21 = $JettonData$_constructor_totalSupply_mintable_owner_content_walletCode($self'total_supply, $self'mintable, $self'owner, $self'jetton_content, $StateInit$_get_code($NeuroWallet$_init_child($global_myAddress(), $self'owner)));
    return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_21);
}

((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), slice) $NeuroMaster$_fun_get_wallet_address((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self, slice $owner) impure inline_ref {
    var (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent)) = $self;
    var ($initCode'code, $initCode'data) = $NeuroWallet$_init_child($global_myAddress(), $owner);
    var $fresh$ret_22 = $global_contractAddress(($initCode'code, $initCode'data));
    return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_22);
}

((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), int) $NeuroMaster$_fun_tvl((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self) impure inline_ref {
    var (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent)) = $self;
    var $fresh$ret_23 = $self'total_assets;
    return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_23);
}

((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), int) $NeuroMaster$_fun_sharePrice((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self) impure inline_ref {
    var (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent)) = $self;
    ifnot ($self'total_supply) {
        var $fresh$ret_24 = 1000000000;
        return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_24);
    }
    var $fresh$ret_25 = (($self'total_assets * 1000000000) / $self'total_supply);
    return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_25);
}

((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), int) $NeuroMaster$_fun_isPaused((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self) impure inline_ref {
    var (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent)) = $self;
    var $fresh$ret_26 = $self'paused;
    return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_26);
}

((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int), int) $NeuroMaster$_fun_totalAssets((int, int, slice, slice, int, cell, int, slice, slice, slice, slice, slice, int) $self) impure inline_ref {
    var (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent)) = $self;
    var $fresh$ret_27 = $self'total_assets;
    return (($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent), $fresh$ret_27);
}

;;
;; Get methods of a Contract NeuroMaster
;;

_ %get_jetton_data() method_id(106029) {
    var self = $NeuroMaster$_contract_load();
    var res = self~$NeuroMaster$_fun_get_jetton_data();
    return $JettonData$_to_external(res);
}

_ %get_wallet_address(slice $owner) method_id(103289) {
    slice $owner = $owner;
    var self = $NeuroMaster$_contract_load();
    var res = self~$NeuroMaster$_fun_get_wallet_address($owner);
    return res;
}

_ %tvl() method_id(68381) {
    var self = $NeuroMaster$_contract_load();
    var res = self~$NeuroMaster$_fun_tvl();
    return res;
}

_ %sharePrice() method_id(86219) {
    var self = $NeuroMaster$_contract_load();
    var res = self~$NeuroMaster$_fun_sharePrice();
    return res;
}

_ %isPaused() method_id(126174) {
    var self = $NeuroMaster$_contract_load();
    var res = self~$NeuroMaster$_fun_isPaused();
    return res;
}

_ %totalAssets() method_id(70882) {
    var self = $NeuroMaster$_contract_load();
    var res = self~$NeuroMaster$_fun_totalAssets();
    return res;
}

;;
;; Routing of a Contract NeuroMaster
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent) = $NeuroMaster$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
        ;; Receive Deposit message
        if (op == 2393262120) {
            var $msg'intent = in_msg~load_uint(8);
            throw_unless(34366, (~ $self'paused));
            var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
            int $value = $ctx'value;
            throw_unless(19776, ($value > 100000000));
            int $depositValue = ($value - 50000000);
            int $minted = $depositValue;
            if (( (($self'total_supply > 0)) ? (($self'total_assets > 0)) : (false) )) {
                $minted = (($depositValue * $self'total_supply) / $self'total_assets);
            }
            $self'total_supply = ($self'total_supply + $minted);
            $self'total_assets = ($self'total_assets + $depositValue);
            var ($initCode'code, $initCode'data) = $NeuroWallet$_init_child($global_myAddress(), $ctx'sender);
            $global_send($SendParameters$_constructor_to_value_mode_bounce_body_code_data($global_contractAddress(($initCode'code, $initCode'data)), 50000000, 0, true, $TokenTransferInternal$_store_cell($TokenTransferInternal$_constructor_queryId_amount_from_response_destination_forward_ton_amount_forward_payload(0, $minted, $global_myAddress(), $ctx'sender, 0, $global_emptySlice()), begin_cell()), $initCode'code, $initCode'data));
            $NeuroMaster$_contract_store(($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent));
            return ();
        }
        
        ;; Receive TokenBurnNotification message
        if (op == 0x7bdd97de) {
            var $msg'queryId = in_msg~load_uint(64);
            var $msg'amount = in_msg~load_varuint16();
            var $msg'sender = in_msg~load_msg_addr();
            var $msg'response_destination = in_msg~__tact_load_address_opt();
            var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
            var ($initCode'code, $initCode'data) = $NeuroWallet$_init_child($global_myAddress(), $msg'sender);
            throw_unless(34393, ( equal_slices_bits($global_contractAddress(($initCode'code, $initCode'data)), $ctx'sender) ));
            int $tonToReturn = $msg'amount;
            if (( (($self'total_supply > 0)) ? (($self'total_assets > 0)) : (false) )) {
                $tonToReturn = (($msg'amount * $self'total_assets) / $self'total_supply);
            }
            $self'total_supply = ($self'total_supply - $msg'amount);
            $self'total_assets = ($self'total_assets - $tonToReturn);
            $global_send($SendParameters$_constructor_to_value_mode_bounce_body($msg'sender, $tonToReturn, 2, false, __gen_cell_cell_3d46e16f7bc13e8cca0c8dc20ca3555e8380a047dfed6f12d191bd5220d3a4c3()));
            if ((~ null?($msg'response_destination))) {
                $global_send($SendParameters$_constructor_to_value_mode_bounce_body(__tact_not_null($msg'response_destination), 0, 66, false, $TokenExcesses$_store_cell($TokenExcesses$_constructor_queryId($msg'queryId), begin_cell())));
            }
            $NeuroMaster$_contract_store(($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent));
            return ();
        }
        
        ;; Receive ExecuteRebalance message
        if (op == 2591664890) {
            var $msg'queryId = in_msg~load_uint(64);
            var $msg'amount = in_msg~load_varuint16();
            var $msg'destination = in_msg~load_msg_addr();
            var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
            throw_unless(9530, ( equal_slices_bits($ctx'sender, $self'keeper) ));
            throw_unless(34366, (~ $self'paused));
            throw_unless(30942, ($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent)~$NeuroMaster$_fun_isWhitelisted($msg'destination));
            int $maxAmount = (($self'total_assets * $self'maxDelegatePercent) / 100);
            throw_unless(50723, ($msg'amount <= $maxAmount));
            throw_unless(61135, ($msg'amount > 0));
            $global_send($SendParameters$_constructor_to_value_mode_bounce_body($msg'destination, $msg'amount, 2, true, __gen_cell_cell_5a68698decba2c0b1ac0cdf004fb38a5949970689d3bed7cb1e7e31cbc892149()));
            $NeuroMaster$_contract_store(($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent));
            return ();
        }
        
        ;; Receive AutoCompound message
        if (op == 3924287516) {
            var $msg'profitToMint = in_msg~load_varuint16();
            var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
            throw_unless(49729, ( (( equal_slices_bits($ctx'sender, $self'keeper) )) ? (true) : (( equal_slices_bits($ctx'sender, $self'owner) )) ));
            throw_unless(21209, ($msg'profitToMint > 0));
            $self'total_assets = ($self'total_assets + $msg'profitToMint);
            $NeuroMaster$_contract_store(($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent));
            return ();
        }
        
        ;; Receive SetWhitelist message
        if (op == 993790908) {
            var $msg'index = in_msg~load_uint(8);
            var $msg'target = in_msg~load_msg_addr();
            var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
            throw_unless(39281, ( equal_slices_bits($ctx'sender, $self'owner) ));
            throw_unless(50835, ( (($msg'index >= 1)) ? (($msg'index <= 5)) : (false) ));
            if (($msg'index == 1)) {
                $self'whitelist1 = $msg'target;
            }
            if (($msg'index == 2)) {
                $self'whitelist2 = $msg'target;
            }
            if (($msg'index == 3)) {
                $self'whitelist3 = $msg'target;
            }
            if (($msg'index == 4)) {
                $self'whitelist4 = $msg'target;
            }
            if (($msg'index == 5)) {
                $self'whitelist5 = $msg'target;
            }
            $NeuroMaster$_contract_store(($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent));
            return ();
        }
        
        ;; Receive Deploy message
        if (op == 0x946a98b6) {
            var $deploy'queryId = in_msg~load_uint(64);
            ($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent)~$NeuroMaster$_fun_notify($DeployOk$_store_cell($DeployOk$_constructor_queryId($deploy'queryId), begin_cell()));
            $NeuroMaster$_contract_store(($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent));
            return ();
        }
        
    }
    ;; Empty Receiver and Text Receivers
    var text_op = slice_hash(in_msg);
    ;; Receive "Pause" message
    if (text_op == 0x1426f4b2781b50266062a76b40df9ae9835c2d109b7be92e1048e36bc2761d67) {
        var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
        throw_unless(49729, ( equal_slices_bits($ctx'sender, $self'owner) ));
        $self'paused = true;
        $NeuroMaster$_contract_store(($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent));
        return ();
    }
    ;; Receive "Resume" message
    if (text_op == 0x40391f4eaad79adb418bdfd49edad3b17b5e392ce4fafbbf82f21f2055ee0f7d) {
        var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
        throw_unless(49729, ( equal_slices_bits($ctx'sender, $self'owner) ));
        $self'paused = false;
        $NeuroMaster$_contract_store(($self'total_supply, $self'total_assets, $self'owner, $self'keeper, $self'paused, $self'jetton_content, $self'mintable, $self'whitelist1, $self'whitelist2, $self'whitelist3, $self'whitelist4, $self'whitelist5, $self'maxDelegatePercent));
        return ();
    }
    ;; Throw if not handled
    throw(130);
}


() __tact_selector_hack_asm() impure asm """
@atend @ 1 {
        execute current@ context@ current!
        {
            // The core idea of this function is to save gas by avoiding unnecessary dict jump, when recv_internal/recv_external is called
            // We want to extract recv_internal/recv_external from the dict and select needed function
            // not by jumping to the needed function by it's index, but by using usual IF statements.

            }END> b> // Close previous builder, now we have a cell of previous code on top of the stack

            <{ // Start of the new code builder
                SETCP0
                // Swap the new code builder with the previous code, now we have previous code on top of the stack
                swap
                // Transform cell to slice and load first ref from the previous code, now we have the dict on top of the stack
                <s ref@

                // Extract the recv_internal from the dict
                dup 0 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot
                swap

                // Delete the recv_internal from the dict
                0 swap @procdictkeylen idict- drop
                // Delete the recv_external from the dict (it's okay if it's not there)
                -1 swap @procdictkeylen idict- drop
                // Delete the __tact_selector_hack from the dict
                65535 swap @procdictkeylen idict- drop

                // Bring the code builder from the bottom of the stack
                // because if recv_external extraction is optional, and the number of elements on the stack is not fixed
                depth 1- roll
                // Swap with the dict from which we extracted recv_internal and (maybe) recv_external
                swap

                // Check if the dict is empty
                dup null?
                // Store a copy of this flag in the bottom of the stack
                dup depth 1- -roll
                {
                    // If the dict is empty, just drop it (it will be null if it's empty)
                    drop
                }
                {
                    // If the dict is not empty, prepare continuation to be stored in c3
                    <{
                        // Save this dict as first ref in this continuation, it will be pushed in runtime by DICTPUSHCONST
                        swap @procdictkeylen DICTPUSHCONST
                        // Jump to the needed function by it's index
                        DICTIGETJMPZ
                        // If such key is not found, throw 11 along with the key as an argument
                        11 THROWARG
                    }> PUSHCONT
                    // Store the continuation in c3
                    c3 POP
                } cond

                // Function id is on top of the (runtime) stack
                DUP IFNOTJMP:<{
                    // place recv_internal here
                    DROP swap @addop
                }>

                // Bring back the flag, indicating if the dict is empty or not from the bottom of the stack
                depth 1- roll
                {
                    // If the dict is empty, throw 11
                    11 THROWARG
                }
                {
                    // If the dict is not empty, jump to continuation from c3
                    c3 PUSH JMPX
                } cond
            }> b>
        } : }END>c
        current@ context! current!
    } does @atend !
""";

() __tact_selector_hack() method_id(65535) {
    return __tact_selector_hack_asm();
}